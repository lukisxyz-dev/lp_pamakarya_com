node_modules
dist
.astro
.emdash/migrations.json
uploads
data.db
data.db-shm
data.db-wal

# Environment / Wrangler secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# Wrangler local state (created by dev/build)
.wrangler

# Local agent tooling, installed per machine (npx impeccable install).
# The skill ships a platform binary; it is not project source.
.agents/skills/impeccable/
.codex/
