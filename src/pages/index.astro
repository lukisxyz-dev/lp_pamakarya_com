---
/**
 * Landing page.
 *
 * PLACEHOLDER for Step 3: the shell (skip link, header, nav, footer, sticky
 * action bar) lives in Base.astro and is what this step verifies. The six
 * content sections land in Steps 4 and 5, replacing the body below.
 *
 * The previous version of this file linked to `/blog`. That link is gone --
 * the site has no second destination, and a link into a section that is not
 * part of the page is a dead end of exactly the kind the current live site is
 * built from.
 */
import Base from "../layouts/Base.astro";
---

<Base>
	<h1>Pamakarya</h1>
</Base>
